import React from "react";
import { JSX } from "react";
interface Props {
    children: React.ReactNode;
    className?: string;
}
export declare const BigTitle: ({ children, className }: Props) => JSX.Element;
export {};
